---
- name: Copy docker-compose to server
  copy:
    src: docker-compose.yml
    dest: /home/robert/docker-compose.yml
    owner: root
    group: root
    mode: 0644

- name: "Deploy samba-ad Container"
  shell:
    cmd: "docker-compose up -d"